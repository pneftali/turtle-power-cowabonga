For aperoo site administration:

Event types with icons / image upon creation must be named using the  convention below:
	event_icon_{event_type_id}
and will be saved to directory: /public/images/events/icons/